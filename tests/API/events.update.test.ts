import httpMocks from 'node-mocks-http';
import eventsEndpointHandler from '../../pages/api/events';

describe('PUT', () => {
  test.todo('method allowed by API');
  test.todo('returns non-empty string');
});
